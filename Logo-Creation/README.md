
  # SVG Logo Creation

  This is a code bundle for SVG Logo Creation. The original project is available at https://www.figma.com/design/msYNhP4v3u382PlgDn1ofk/SVG-Logo-Creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  